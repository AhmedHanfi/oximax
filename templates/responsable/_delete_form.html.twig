<form method="post" action="{{ path('app_responsable_delete', {'id': responsable.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ responsable.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
