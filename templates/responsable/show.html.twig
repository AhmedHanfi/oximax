{% extends 'base.html.twig' %}

{% block title %}Responsable{% endblock %}

{% block body %}
    <h1>Responsable 555</h1>

    <table class="table" id="datatable">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ responsable.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ responsable.Nom }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ responsable.Mail }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ responsable.Prenom }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ responsable.telephone }}</td>
            </tr>
            <tr>
                <th>Adress</th>
                <td>{{ responsable.adress }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-warning" href="{{ path('app_responsable_index') }}">Retour</a>

    <a class="btn btn-outline-success" href="{{ path('app_responsable_edit', {'id': responsable.id}) }}">Editer</a>

    {{ include('responsable/_delete_form.html.twig') }}
{% endblock %}
