<form method="post" action="{{ path('app_docteur_delete', {'id': docteur.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ docteur.id) }}">
    <button class="btn btn-danger" class="btn">Supprimer</button>
</form>
